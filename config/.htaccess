# Заборона будь-якого прямого веб-доступу до конфігураційних файлів.
# Секрети, ключі та параметри БД не повинні бути доступні ззовні.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Deny,Allow
    Deny from all
</IfModule>
Options -Indexes
