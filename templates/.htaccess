# Заборона прямого веб-доступу до шаблонів.
# Ці файли призначені виключно для включення через PHP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Deny,Allow
    Deny from all
</IfModule>
Options -Indexes
