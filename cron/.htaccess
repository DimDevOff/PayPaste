# Заборона прямого веб-доступу до cron-скриптів.
# Ці скрипти призначені виключно для запуску через CLI / crontab.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Deny,Allow
    Deny from all
</IfModule>
Options -Indexes
